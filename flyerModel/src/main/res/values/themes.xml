<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Flyer" parent="Theme.AppCompat.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/Primary</item>
        <item name="colorPrimaryVariant">@color/P_Light</item>
        <item name="colorOnPrimary">@color/TextOnP</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/Secondary</item>
        <item name="colorSecondaryVariant">@color/S_Light</item>
        <item name="colorOnSecondary">@color/TextOnS</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.StartingWindowTheme" parent="Theme.Flyer">

        <!-- 非常重要，不设置，后续设置无效 -->
        <item name="android:windowTranslucentStatus">true</item>

    </style>

</resources>